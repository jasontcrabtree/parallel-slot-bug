export default function Test() {
    return (
        <p>
            Test page. Works correctly in development, once deployed flashes the component for a second then shows a 404 page due to no default or matching @team parallel slot.
        </p>
    )
}